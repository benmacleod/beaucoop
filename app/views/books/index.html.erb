<%= table_for @books, html: { class: 'datatable table table-hover table-bordered' } do-%>
  <% columns :id, :title, :author, :publisher, :edition, :category, :isbn, :price, :in_shop %>
  <% if can? :destroy, Book %>
    <% column :delete_button, title: '' %>
  <% end %>
<% end %>
<br/>
<br/>
<b>Many more books available in our shop space in Union House - drop in to browse!</b>
<%= image_tag 'Bookmark.png', style: 'margin-left: 0px' %>

